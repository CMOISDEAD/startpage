import styled from 'styled-components';
import { gruvbox } from './Colors';

export const Container = styled.div`
  max-width: 20vw;
  border: 1px solid ${gruvbox.foreground};
  color: ${gruvbox.orange};
  text-align: center;
  padding: 1rem;
`
